name: Export current Phase 13 working tree

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/phase13-current-source-export.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase13-current-source-${{ github.event_name }}-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          fetch-depth: 0
      - name: Archive source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/phase13-current-source
          tar --exclude='.git' --exclude='artifacts' -czf artifacts/phase13-current-source/source.tar.gz .
          sha256sum artifacts/phase13-current-source/source.tar.gz > artifacts/phase13-current-source/sha256.txt
      - name: Upload source
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: phase13-current-source-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/phase13-current-source/
          if-no-files-found: error
          retention-days: 7
