# Source-Withholding Notice

This is a results-and-authority-evidence publication only.

The public archive excludes all Phase 15 and Phase 16 implementation material, including Lean and Python source, workflows, tests, model weights, candidate packages, private hidden challenges, answer stores, mutation programs, and private runtime payloads. The public source implementation remains frozen through Formal Core v4 Gate E and Executable Core v4 Phase 14.

The published JSON reports are exact byte copies extracted from the listed authoritative GitHub Actions artifacts. The published ZIP archives are exact byte copies of selected safe original artifacts. Full private capture/bundle archives that cross the source-withholding boundary are not published; their immutable artifact IDs and SHA-256 digests remain recorded in `release_manifest.json` and `ARTIFACT_INVENTORY.md`.
