# Public benchmark adapter scaffolds

These files intentionally do not fake public benchmark scores. Use official benchmark harnesses to produce score artifacts, then wrap those artifacts with `certified_public_subset_harness.py --benchmark external-manifest`.
