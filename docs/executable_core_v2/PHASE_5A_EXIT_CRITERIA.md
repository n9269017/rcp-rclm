# Phase 5A exit criteria

Phase 5A closed at implementation head
`ca6b7edd7df34b479be031c8cd87104db67614e9` in workflow run `29309437018`.
The retained validation record establishes:

- [x] Phase 0 contract validation passes.
- [x] Phase 1 through Phase 4 regression suites pass.
- [x] Python source quality reports no placeholder or forbidden backend issue.
- [x] Linux, Windows, and macOS execute the separate-process generator suite.
- [x] The worker input schema rejects checker source, trust anchors, previous manifests, promotion ledgers, and reference answers.
- [x] The proposal schema contains no certificate, candidate, successor, or acceptance field.
- [x] The worker source guard rejects direct file, process, network, privileged-import, reflective built-in, and dynamic module-table capabilities.
- [x] The worker runs in a separate isolated process with no file arguments, network endpoints, or granted write handles.
- [x] The `initial` predecessor deterministically emits only the bounded `improve` word.
- [x] The `target` predecessor deterministically emits only the bounded `stabilize` word.
- [x] Word depth, proof length, proposal count, and resource bounds match the Lean seed library.
- [x] Two fresh worker executions produce identical bytes, proposals, and process reports.
- [x] Certificate construction is performed outside the worker.
- [x] Selection is deterministic and consumes no model score or reference answer.
- [x] The successor is derived by applying the selected update; no generator successor field or manual successor output is consumed.
- [x] The generator predecessor-manifest hash matches the independently reconstructed checker predecessor manifest.
- [x] The pinned Formal Core project builds.
- [x] Generated Lean source passes the pre-compilation `sorry`/`sorryAx`/`admit`/local-axiom gate.
- [x] Both reference transitions receive accepting pinned Lean reports.
- [x] Both reference transitions receive accepting Phase 4 hardened checker reports.
- [x] Every input, output, stage result, Lean report, checker report, and final report hash is retained.
- [x] The workflow closure artifact identifies the checked pull-request merge revision, while artifact metadata binds the run to the implementation head.

The following remain false after Phase 5A:

```text
generator trust
candidate promotion authorization
real filesystem/package realization
selector ranking beyond the single bounded proposal
open-ended search or program synthesis
LLM or scaffold proposal generation
PyTorch proposal or training-driven updates
independent replay without the original generator
external benchmark claims
general noncommuting quantum semantics
autonomous or unbounded RSI
```

A clean Phase 5A closure licenses Phase 5B untrusted-generator experiments and Phase 6
realizer/package-builder development. Neither path enters the promotion trust boundary.
