# Phase 13 — Independent replay and adversarial closure

## Phase 13A selected boundary

Phase 13A freezes the replay-only trust boundary and the complete selected attack registry before implementing the full `M0 → M4` reproducer.

The replay boundary permits only retained evidence. Training workers, generator workers, planner execution, proposal scaffolds, optimizer execution, PyTorch, dynamic loading, and direct candidate mutation are outside the reproducer.

The executable Phase 13A record requires:

```text
training invocations = 0
generator invocations = 0
planner invocations = 0
forbidden learned modules loaded = []
forbidden worker paths retained = []
```

The source guard rejects imports of training/generator backends, dynamic loading, subprocess execution, nondeterministic randomness, network access, and direct generator calls from the replay implementation.

## Frozen adversarial registry

The selected attack suite contains all 21 Phase 13 attacks:

1. held-out-task leakage;
2. hidden reference-answer access;
3. task-manifest substitution;
4. capability-ledger forgery;
5. deletion of previously solved tasks;
6. evaluation-set cherry picking;
7. goal-drift substitution;
8. model/tokenizer mismatch;
9. adapter/base mismatch;
10. architecture-manifest forgery;
11. generator-package substitution;
12. proposal generated by an unbound external model;
13. trainer replay disguised as evaluation;
14. poisoned retrieval index;
15. self-reported model capability;
16. benchmark memorization markers;
17. resource-accounting evasion;
18. dynamic code loading;
19. post-check generator mutation;
20. parent-chain substitution;
21. replay requiring original learned modules.

Each attack is executed twice against the canonical selected replay claim. Phase 13A closes only when both executions are byte-hash deterministic and the expected fail-closed reason is present.

## Dependency audit

Phase 13A independently audits whether the merged Phase 12 tree contains the complete final-transition source, schema, and workflow surface. This audit is reported separately from replay-boundary closure. It is not permitted to silently infer missing Phase 12 evidence from a PR description or prior chat statement.

## Claim boundary

Phase 13A establishes the canonical worker-free replay contract, retained-evidence manifest, source guard, zero-invocation counters, and complete selected attack grammar.

It does not yet establish full independent replay of every accepted transition and retained rejection. Accordingly:

```text
phase13a_slice_closed = true
phase13_exit_closed = false
```
