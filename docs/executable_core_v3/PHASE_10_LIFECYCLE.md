# Phase 10 lifecycle integration

This document tracks the final Phase 10 trust-boundary slice.

The learned compact-model candidate is realized through the unchanged Runtime v2 Phase 6 boundary. The host-owned selection modifies only files beneath the declared `model_weights` component, the public candidate verifier reopens the result, and the rollback archive must restore the predecessor payload byte-for-byte.

The accepted candidate is then subject to independent task, information, Lean, hardened-checker, and Gate D validation before the immutable Runtime v2 Phase 7 store may atomically replace the active package. Independent replay must reconstruct the candidate and all authoritative evidence with zero training and generator invocations.

This record does not declare Phase 10 complete until the complete lifecycle closes at one exact source head on Linux, Windows, macOS, and pinned Lean.
